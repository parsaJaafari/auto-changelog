{
  "name": "auto-change-log",
  "version": "3.1.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "release": "standard-version",
    "bump:patch": "npm run release -- --release-as patch",
    "bump:minor": "npm run release -- --release-as minor",
    "bump:major": "npm run release -- --release-as major"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^17.0.3",
    "@commitlint/config-conventional": "^17.0.3",
    "husky": "^8.0.1",
    "standard-version": "^9.5.0"
  },
  "dependencies": {
    "express": "^4.18.1",
    "prettier": "^2.7.1"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
